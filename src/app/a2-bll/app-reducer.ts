const initialState: any = {}

export const appReducer = (state: any = initialState, action: AppActionType): any => {
    switch (action.type) {
        default:
            return state
    }
}

//types
export type AppActionType = any